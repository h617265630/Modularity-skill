# ============================================================================
# Modularity-skill Python Dependencies
# ============================================================================

# Core dependencies (optional - for enhanced linting/type checking)
pylint>=3.0.0
black>=23.0.0
mypy>=1.7.0

# Testing framework (required for running tests)
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0  # For TestClient in FastAPI tests

# Code quality (optional)
flake8>=6.0.0
isort>=5.12.0
